import { Metadata } from "next"

export const metadata: Metadata = {
   title: "Log in Account",
   description: "log in account"
}

export default function LoginPage() {
   return <div>Log in Account</div>
}
